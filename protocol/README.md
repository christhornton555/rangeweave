# Protocol

**Status: NEXT WORK PACKAGE.**

See [`docs/protocol.md`](../docs/protocol.md). This directory will contain the normative byte-level packet specification plus fixtures. The semantic format must be independent of Pico, MicroPython, USB and Python.
