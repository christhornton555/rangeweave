# PCB

**Status: PLANNED.**

Do not design the custom compact PCB until the packet/host architecture and first mapping experiments have exposed the real bandwidth, compute, power and calibration requirements.

The eventual board should preserve the logical sensor/clock/packet boundaries and should not assume Pico-specific GPIO numbering or USB as the only transport.
