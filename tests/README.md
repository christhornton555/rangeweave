# Cross-project tests

Future cross-cutting tests belong here when they span packages/platforms, for example:

- protocol corruption/resynchronization fixtures;
- Python <-> Kotlin conformance outputs;
- replay regression tests;
- coordinate-transform golden examples;
- reference-dataset metric regression tests.
