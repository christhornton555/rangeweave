# Tools

- `fetch_vl53l5cx_firmware.py` - downloads (but does not redistribute) the third-party VL53L5CX firmware blob and prints/verifies SHA-256.

For a tagged reproducibility release, prefer an immutable upstream URL plus an expected checksum rather than an unpinned branch URL.
